{
  "name": "opencode-sandbox",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Project TypeScript nho de OpenCode agent lam viec that o Milestone 0",
  "engines": { "node": ">=22" },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
